/*
package com.example.habitapp.Model;

import static org.junit.Assert.*;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class ReminderTest {
    private Reminder reminder;

    @Before
    public void setUp() throws Exception {
        reminder = new Reminder();
    }

    @Test
    public void testSetAndGetReminderTitle(){
        String title = "Testing title";
        reminder.setReminderTitle(title);
        assertEquals(reminder.getReminderTitle(), title);
    }

    @Test
    public void testSetAndGetReminderNote(){
        String note = "This is a note used to test this reminder";
        reminder.setReminderNote(note);
        assertEquals(reminder.getReminderNote(), note);
    }

    @Test
    public void testSetAndGetReminderUrl(){
        String url = "This is the url for this reminder";
        reminder.setReminderUrl(url);
        assertEquals(reminder.getReminderUrl(), url);
    }

    @Test
    public void testSetAndGetReminderDateAndTime(){
        String dateAndTime = "This is the date and time for this reminder";
        reminder.setReminderDateAndTime(dateAndTime);
        assertEquals(reminder.getReminderDateAndTime(), dateAndTime);
    }

    @Test
    public void testSetAndGetReminderRepeat(){
        String repeat = "Daily";
        reminder.setReminderRepeat(repeat);
        assertEquals(reminder.getReminderRepeat(), repeat);
    }

    @Test
    public void testSetAndGetReminderLocation(){
        String location = "32 Donna Street";
        reminder.setReminderLocation(location);
        assertEquals(reminder.getReminderLocation(), location);
    }

    @Test
    public void testSetAndGetReminderFlagged(){
        reminder.setReminderFlagged(true);
        assertTrue(reminder.isReminderFlagged());
        reminder.setReminderFlagged(false);
        assertFalse(reminder.isReminderFlagged());
    }

    @Test
    public void testSetAndGetReminderPriority(){
        String priority = "1";
        reminder.setReminderPriority(priority);
        assertEquals(reminder.getReminderPriority(), priority);
    }

    @Test
    public void testSetAndGetReminderImage(){
        String image = "This is the image ";
        reminder.setReminderImage(image);
        assertEquals(reminder.getReminderImage(), image);
    }

    @Test
    public void testSetAndGetReminderCompleted(){
        reminder.setReminderCompleted(true);
        assertTrue(reminder.isReminderCompleted());
        reminder.setReminderCompleted(false);
        assertFalse(reminder.isReminderCompleted());
    }

    @After
    public void tearDown() throws Exception {
        reminder = null;
        assertNull(reminder);
    }
}*/
